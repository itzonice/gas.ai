import { ScreenPlaceholder } from "../../components/ScreenPlaceholder";

export default function TodayScreen() {
  return (
    <ScreenPlaceholder
      title="Today"
      description="What's due, what to review, and what to do next."
    />
  );
}
